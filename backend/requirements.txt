# Web framework (ADR-03)
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.32

# Config
python-dotenv==1.2.2

# Document loaders + text splitting only — not LangChain's chains/agents (ADR-05)
langchain-community==0.3.27
langchain-text-splitters==0.3.9
pypdf==6.17.0
docx2txt==0.8

# CPU-only torch build (Render's free-tier instance is CPU-only, 512MB RAM).
# Pinned explicitly so pip's resolver satisfies sentence-transformers' unpinned
# `torch>=1.11.0` requirement with this wheel instead of defaulting to the
# standard PyPI torch build, which pulls in several GB of unused NVIDIA
# CUDA runtime packages (nvidia-cublas, nvidia-cudnn, etc.) that OOM-kill the
# process on startup. Version must stay in sync with whatever plain `torch`
# would otherwise resolve to — check https://download.pytorch.org/whl/cpu/torch/
# (or `pip index versions torch --index-url https://download.pytorch.org/whl/cpu`)
# for available versions before bumping.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.14.0+cpu

# Local embeddings, no API/rate limit (ADR-06)
sentence-transformers==3.3.1

# Vector store (ADR-07)
qdrant-client==1.12.1

# LLM (ADR-08)
groq==0.15.0
