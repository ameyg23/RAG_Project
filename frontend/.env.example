# Copy to frontend/.env for local development. Never commit a real .env file.
# Full details: docs/ENVIRONMENT.md
# Anything prefixed VITE_ is public (baked into the built bundle) — never put a secret here.

VITE_API_BASE_URL=http://localhost:8000
